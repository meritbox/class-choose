<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dbcourse.classchoose.mapper.TeacherMapper">
<select id="getAll" resultType="com.dbcourse.classchoose.entity.DTO.TeacherDTO">
    select teacher.tno tno,teacher.tname tname,teacher.sex sex,teacher.dno dno,department.dname department
        from teacher left join department on teacher.dno=department.dno
</select>
</mapper>
